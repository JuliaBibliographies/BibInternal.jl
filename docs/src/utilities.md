Utility helpers are intentionally small and focused. They support the parser and
the canonical model, but they are not meant to be the main user-facing surface.

```@contents
```

```@autodocs
Modules = [BibInternal]
Pages   = ["constant.jl", "utilities.jl"]
```
